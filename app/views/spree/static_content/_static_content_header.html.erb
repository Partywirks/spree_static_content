<% header_links = Spree::Page.by_store(current_store).visible.header_links
	if header_links.count > 4	 
%>

		<li class="dropdown user user-menu">
		  <a href="#" class="dropdown-toggle" data-toggle="dropdown">
		    <%= Spree.t(:pages) %> <i class="caret"></i>
		  </a>
		  <ul class="dropdown-menu">
		  	<%= render :partial => "spree/static_content/static_content_list", :collection => header_links, :as => :page %>
			</ul>
		</li>


<%  	
	else
%>

	
			<%= render :partial => "spree/static_content/static_content_list", :collection => header_links, :as => :page %>


<%			
	end
%>

